/*
    1. Boolean: Como mencionado anteriormente, é um tipo de dado que representa verdadeiro ou falso. 
    É frequentemente usado em estruturas de controle, como condicionais e loops, 
    para controlar o fluxo do programa.

    2. Null: Representa a ausência intencional de qualquer valor. 
    É diferente de `undefined`, pois é atribuído explicitamente.

    3. Undefined: Representa uma variável que foi declarada, mas ainda 
    não foi atribuída com um valor. Pode ocorrer quando uma variável é declarada sem 
    atribuição ou quando uma função não retorna nenhum valor explicitamente.

*/

let valor = null; // Valor é explicitamente definido como nulo
let variavelIndefinida; // Variável é declarada, mas não atribuída, então seu valor é undefined